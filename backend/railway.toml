[build]
builder = "DOCKERFILE"
dockerfilePath = "../Dockerfile"

[deploy]
startCommand = "python -m uvicorn backend.api.server:app --host 0.0.0.0 --port 8001"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "jarvis-backend"
